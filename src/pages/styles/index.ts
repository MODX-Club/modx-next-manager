import styled from 'styled-components'
import { FormControlStyled } from '@procraft/ui/dist/components/form/FormControl/styles'

export const ToolbarStyled = styled.div`
  ${FormControlStyled} {
    transform: scale(0.8);
  }
`

export const PageStyled = styled.section`
  padding: 20px;
`
