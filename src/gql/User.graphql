query me($where: UserWhereUniqueInput!) {
  me(where: $where) {
    id
  }
}

mutation signin(
  $login_context: String
  $username: String!
  $password: String!
) {
  signin(
    login_context: $login_context
    username: $username
    password: $password
  ) {
    token
    userId
  }
}
